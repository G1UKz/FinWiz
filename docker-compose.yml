services:
  db:
    image: postgres:16-alpine
    container_name: finwiz_db
    environment:
      POSTGRES_USER: finwiz
      POSTGRES_PASSWORD: finwiz
      POSTGRES_DB: finwiz
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
